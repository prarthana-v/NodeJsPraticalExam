<%- include('../components/header') %>
<div class="page-wrapper">
  <!-- ============================================================== -->
  <!-- Bread crumb and right sidebar toggle -->
  <!-- ============================================================== -->
  <div class="page-breadcrumb" style="height: 100%">
    <div class="row">
      <div class="col-12 d-flex no-block align-items-center">
        <h4 class="page-title ms-2 my-3">Category</h4>
      </div>

      <div class="container-fluid">
        <% if(locals.success !="" ) { %>
        <div
          class="alert alert-success alert-dismissible fade show"
          role="alert"
        >
          <%= locals.success %>
        </div>
        <% } %>
        <div class="row">
          <div class="col-12">
            <div class="card">
              <div class="card-body">
                <div class="d-flex justify-content-between">
                  <h5 class="card-title">Table</h5>

                  <a href="/country/add" class="btn btn-success">Add</a>
                </div>
              </div>

              <table class="table">
                <thead>
                  <tr>
                    <th scope="col">#</th>
                    <th scope="col">country</th>
                    <th scope="col">Status</th>
                    <th>Action</th>
                  </tr>
                </thead>
                <tbody>
                  <% country.map((c,i)=>{ %>
                  <tr>
                    <th scope="row"><%= ++i %></th>
                    <td><%= c.country %></td>
                    <td>
                      <a
                        href="/country/changeStatus?status=<%= c.status %>&id=<%= c.id %>"
                        class="<% if(c.status == `active`) { %> btn btn-success <% } else { %> btn btn-danger <% } %>"
                      >
                        <%= c.status %>
                      </a>
                    </td>
                    <td>
                      <a
                        href=" /country/delete?id=<%=c.id %>"
                        class="btn btn-danger btn-sm"
                      >
                        <i class="fa fa-trash"></i>
                      </a>
                      <a
                        href="/country/edit?id=<%=c.id %>"
                        class="btn btn-primary btn-sm"
                      >
                        <i class="fa fa-edit"></i>
                      </a>
                    </td>
                  </tr>
                  <% }) %>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- ============================================================== -->
  <!-- End footer -->
  <!-- ============================================================== -->
</div>

<%- include('../components/footer') %>
